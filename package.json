{
  "name": "gestor-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Plataforma de migração do GestorLoc Delphi para Web.",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format": "prettier --write .",
    "check": "pnpm lint && pnpm test && pnpm build",
    "validate:delphi-fixtures": "pnpm --filter @gestor/delphi-parser validate:components && pnpm --filter @gestor/delphi-parser validate:fixture-components && pnpm --filter @gestor/delphi-parser validate:fixture:produtos && pnpm --filter @gestor/delphi-parser validate:fixture:parceiros && pnpm --filter @gestor/delphi-parser validate:fixture:grupo-produtos && pnpm --filter @gestor/delphi-parser validate:fixture:grupo-parceiros"
  },
  "devDependencies": {
    "@types/node": "latest",
    "prettier": "latest",
    "typescript": "latest"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
